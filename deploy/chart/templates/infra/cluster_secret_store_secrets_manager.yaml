apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: hello-world-secrets-manager
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.infra.argocd.syncWave | quote }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.aws.region }}
      auth:
        jwt:
          serviceAccountRef:
            name: eso
            namespace: default
