{{ include "ticketmaster.migrationJob" (dict
  "service" "hello-world"
  "alembicTarget" "contract@head"
  "syncWave" .Values.services.helloWorld.migrations.contract.meta.argocd.syncWave
  "imageRepository" .Values.services.helloWorld.image.repository
  "commitSha" .Values.commitSha
  "externalSecretName" "hello-world"
) }}
