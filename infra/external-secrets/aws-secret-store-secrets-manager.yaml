---
# Source: ticketmaster/templates/infra/external-secrets/aws-secret-store-secrets-manager.yaml
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secret-store-secrets-manager
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  provider:
    aws:
      service: SecretsManager
      region: eu-central-1
      auth:
        jwt:
          serviceAccountRef:
            name: eso
            namespace: default
