import './styles.css';

export function App() {
    return (
        <main>
            <h1>Hello from ticketmaster frontend</h1>
            <p>Built with React + Vite. Deployed via ECS Express.</p>
        </main>
    );
}
